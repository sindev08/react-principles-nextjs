# `src/lib/`

Infrastructure and configuration code. Contains the API client factory (`createApiClient`), a pre-configured API instance for DummyJSON, centralized endpoint definitions, TanStack Query client setup, and query key factories. This is the "plumbing" layer — features import from here to make API calls and manage server state.
