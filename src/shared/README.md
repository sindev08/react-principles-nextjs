# `src/shared/`

Cross-feature shared code. Contains reusable components (ErrorBoundary, LoadingState), hooks (useDebounce, useLocalStorage), app-wide Zustand stores, shared TypeScript types, and utility functions. Code here is used by multiple features. If something is only used by one feature, it belongs in that feature's directory instead.
