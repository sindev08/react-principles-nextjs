// Test setup file — loaded before every test via vitest.config.ts `setupFiles`
// Adds custom DOM matchers like toBeInTheDocument(), toHaveTextContent(), etc.
import "@testing-library/jest-dom/vitest";
